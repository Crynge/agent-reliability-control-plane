{
  "name": "agent-control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "Reliability control plane prototype for AI agents",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server/server.js && node --check server/lib/controlPlane.js && node --check server/lib/store.js && node --check apps/dashboard/app.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
